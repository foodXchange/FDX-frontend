
import { Route } from 'react-router-dom';

export const OrderRoutes = () => {
  return (
    <>
      <Route path="/" element={<div>Order Dashboard</div>} />
    </>
  );
};

export default OrderRoutes;
